# Copie para .env e ajuste os valores
# MATOUR_BASE_URL=https://mimatourviagens.suareservaonline.com.br
# PORT=3000  (usado pela API; FluxiChat deve chamar esta mesma URL, ex: http://localhost:3000)
# COLLECTOR_TIMEOUT_MS=15000
# COLLECTOR_MAX_RETRIES=3

# Enquanto o site estiver atrás do Cloudflare, use mock para desenvolver sem acessar o site
# USE_MOCK_DATA=true

# Opcional: HTML da listagem salvo em arquivo (para ajustar seletores sem bater no site)
# FIXTURE_HTML_PATH=fixtures/mimatour-page.html
# FIXTURE_HTML_FULL_LIST=fixtures/mimatour-ver-todos.html

# Puppeteer: true por padrão (navegador real para contornar Cloudflare). Use false para pular (ex.: servidor sem Chrome)
# USE_PUPPETEER=true
